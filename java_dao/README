For test execution you must specify PROJECT_ROOT 
environment variable equals to full path to directory where
this project is placed.

Also you need to copy test.properties.example to test.properties and
specify your MySQL connection parameters.
